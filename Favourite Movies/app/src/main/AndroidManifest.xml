<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.himanshudhawale.myfavoritemovies">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddMovieActivity"
            android:label="@string/addMovieActivity" />
        <activity
            android:name=".EditMovieActivity"
            android:label="@string/editMovieActivity" />
        <activity android:name=".MoviesByYear">
            <intent-filter>
                <action android:name="com.example.himanshudhawale.myfavoritemovies.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"></category>


            </intent-filter>

        </activity>>
        <activity android:name=".MovieByRating">
            <intent-filter>
                <action android:name="com.example.himanshudhawale.myfavoritemovies.intent.action.SEND1"/>
                <category android:name="android.intent.category.DEFAULT"></category>


            </intent-filter>

        </activity>


    </application>

</manifest>